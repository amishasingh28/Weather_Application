name: Node.js Package

on:
  release:
    types: [created]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # Checkout the code
      - uses: actions/checkout@v4

      # Set up Node.js and install dependencies
      - uses: actions/setup-node@v4
        with:
          node-version: 20

      # Install dependencies using npm ci (clean install)
      - run: npm ci

      # Run tests to ensure the package works as expected
      - run: npm test

  publish-gpr:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      # Checkout the code again in case of context loss
      - uses: actions/checkout@v4

      # Set up Node.js and configure GitHub registry for publishing
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: https://npm.pkg.github.com/

      # Install dependencies again
      - run: npm ci

      # Publish the package to GitHub Packages
      - run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.TOKEN }}

  # # Optional: Deployment Job (e.g., Deploy to AWS, Vercel, Netlify, etc.)
  # deploy:
  #   needs: publish-gpr
  #   runs-on: ubuntu-latest
  #   steps:
  #     # Checkout the code again
  #     - uses: actions/checkout@v4

  #     # Example: Deploy to AWS Amplify (this can vary based on your deployment service)
  #     - name: Deploy to AWS Amplify
  #       run: |
  #         # Set up AWS CLI
  #         aws configure set aws_access_key_id ${{ secrets.AWS_ACCESS_KEY_ID }}
  #         aws configure set aws_secret_access_key ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  #         aws configure set default.region us-east-1
          
  #         # Example AWS CLI command to deploy (customize according to your setup)
  #         aws amplify start-deployment --app-id <app_id> --branch-name main

